class Example:
    """Example is to see if doc gen can pick up a new file.
    """
    def some_func(self, arg: str) -> None:
        """Some func prints the arg.

        Args:
            arg (str): something to print
        """
        print(arg)
